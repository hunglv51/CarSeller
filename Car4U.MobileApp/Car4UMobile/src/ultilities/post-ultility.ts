import { Post } from "../models/post";

export class PostUltility
{
    groupBy(posts: Array<Post>, field: string)
    {
        // return posts.reduce((prevs, next) => {
        //     prevs[next.]
        // }, {});
    }
}